development:
  host: localhost
  database: printer

test:
  host: localhost
  database: printer_test

staging:
  host: localhost
  database: printer

# set these environment variables on your prod server
production:
  host: localhost
  database: printer
